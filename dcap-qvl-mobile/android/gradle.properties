# Required because the instrumented (androidTest) suite depends on AndroidX
# (androidx.test). The main library itself only uses JNA + coroutines.
android.useAndroidX=true
android.nonTransitiveRClass=true
